name: mix_parlay_tracker
description: Mix Parlay Tracker - Track picks, results, bankroll & win rate, generate parlay, AI analysis
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  provider: ^6.1.0
  path_provider: ^2.1.0
  json_annotation: ^4.9.0
  http: ^1.2.0
  encrypt: ^5.0.3
  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.14.0
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
